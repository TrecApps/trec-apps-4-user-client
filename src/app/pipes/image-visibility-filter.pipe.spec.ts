import { ImageVisibilityFilterPipe } from './image-visibility-filter.pipe';

describe('ImageVisibilityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageVisibilityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
