import { ImageAlbumFilterPipe } from './image-album-filter.pipe';

describe('ImageAlbumFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageAlbumFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
